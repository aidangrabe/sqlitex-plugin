# SQLitex IntelliJ Plugin

A plugin for querying an SQLite database on a connected Android emulator from within Android Studio.

# Development

Open in IntelliJ, make some changes and run the `runIde` Gradle task.